<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="akhil.indetermenancy_of_structure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Kinematic_activity" />
        <activity android:name=".Static_activity" />
        <activity android:name=".Pin_joint_activity" />
        <activity android:name=".Rigid_joint_activity" />
        <activity android:name=".Hybrid_activity"></activity>
    </application>

</manifest>